#include<stdio.h>
void main()
{
	int t,i;
	int h,w,n;
	int*num;
	scanf("%d",&t);
	num=malloc(sizeof(int)*t);
	
	for(i=0; i<t; i++)
	{
		scanf("%d%d%d",&h, &w, &n);
		for(j=0; j<h; j++)
		for(k=0; k<w;k++)
		if(i*h+)
		num[i]= ;
	}
}
